Implement a single source of truth for KareBraids services across Home, Gallery, and Booking. Create `client/src/data/services.js` or equivalent with canonical service shape and selectors, refactor pages/components to import those selectors, preserve booking API contract, gallery filtering/modal/lazy/fallback/accessibility behavior, and update Vitest coverage. Run `npm test --prefix client` and `npm run build --prefix client`.
