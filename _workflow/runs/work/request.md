# Active Request

Redesign the KareBraids About page using Afro Hair Trends screenshots as layout inspiration while keeping KareBraids premium Afro-luxury branding.

Key requirements:
- Replace the current About page with a warm, premium, feminine, trustworthy, service-led page.
- Use a full-width dark hero banner, Meet Karen two-column story section, four trust cards, dark image CTA/experience banner, specialties grid, testimonials, trust stats, and final CTA.
- Keep internal CTAs route-safe to `/booking` and `/gallery`.
- Use React, Vite, Tailwind CSS, reusable components, static arrays, accessible semantic HTML, proper alt text, and existing routing.
- Do not add Opening Hours to About.
- Build must pass with `npm run build`; run/fix tests if present.
