# Optional. Leave unset for the default same-origin /api endpoint.
# Set the complete API prefix only when the backend is hosted separately.
VITE_API_URL=http://localhost:5000/api

# Optional Vite development proxy target. The default is http://localhost:5000.
VITE_API_PROXY_TARGET=http://localhost:5000
